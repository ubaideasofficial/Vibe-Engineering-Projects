# Neon Hover Runner

A browser-based, procedural neon hoverboard runner built with Next.js and React Three Fiber.

## Run locally

```bash
npm install
npm run dev
```

Open `http://localhost:3000`. Use **A/D** or arrow keys to change lanes, **Space/Up** to jump, and **Shift/Down** to slide. Mobile swipes are supported.
